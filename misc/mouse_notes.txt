[s.name for s in win._toDraw if s.contains(mouse.lastPos)]

# get number of last pressed button:

next((s.name for s in win._toDraw if s.contains(mouse.lastPos)), None)[-1]

mouse_names = sorted(set(
    name.split('.')[1]
    for name in thisExp.dataNames
    if '.mouse' in name
))
mouse_names

current_mouse=mouse_names[-1]

stim = next((s for s in win._toDraw if s.contains(mouse.lastPos)), None)
button_number = stim.name[-1] if stim else None
response = stim.text if stim else None
button_number, response



event.Mouse

event.Mouse.getPos()